spring:
  application:
    name: expense-budgeting

  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate

app:
  recurring-transactions:
    cron: 0 0 1 * * *